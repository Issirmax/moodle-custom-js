document.addEventListener('DOMContentLoaded', function () { const editToggle = document.querySelector('input[id$="-editingswitch"]'); if (!editToggle) return; const container = editToggle.closest('.input-group'); /* Bloquear durante 2.8 segundos tras carga*/ if (container) { container.classList.add('temporarily-disabled-wrapper'); setTimeout(() => { container.classList.remove('temporarily-disabled-wrapper'); }, 2800); } let clicked = false; editToggle.addEventListener('click', function () { if (clicked || container?.classList.contains('temporarily-disabled-wrapper')) return; clicked = true; }); });